# FastAPI settings
API_HOST=0.0.0.0
API_PORT=8000

# Postgres
# Docker 環境：postgresql://postgres:postgres@postgres:5432/thesisflow
# 本地開發：postgresql://postgres:postgres@localhost:5432/thesisflow
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/thesisflow

# 前端 Domain URL（用於 CORS，可選）
# 開發環境：留空或使用 http://localhost:3000
# 生產環境：https://your-frontend-domain.com
FRONTEND_DOMAIN=

# 前端 Port（預設 3000）
FRONTEND_PORT=3000

# 後端 Domain URL（可選）
# 開發環境：留空或使用 http://localhost:8000
# 生產環境：https://your-backend-domain.com
BACKEND_DOMAIN=

# 後端 Port（預設 8000）
BACKEND_PORT=8000

# MinIO (S3-compatible)
# 請設置您的 MinIO 服務端點（例如：localhost:9000 或 your-minio-endpoint:9000）
MINIO_ENDPOINT=your-minio-endpoint:9000
# 請設置您的 MinIO Access Key
MINIO_ACCESS_KEY=your_minio_access_key
# 請設置您的 MinIO Secret Key
MINIO_SECRET_KEY=your_minio_secret_key
# 請設置您的 MinIO Bucket 名稱
MINIO_BUCKET=your-bucket-name
# 是否使用 SSL（true/false）
MINIO_USE_SSL=false

# Azure OpenAI
# 請設置您的 Azure OpenAI 端點（例如：https://your-endpoint.cognitiveservices.azure.com）
AZURE_OPENAI_ENDPOINT=https://your-endpoint.cognitiveservices.azure.com
# 請設置您的 Azure OpenAI API Key
AZURE_OPENAI_API_KEY=please_set_me
# 請設置您的 Azure OpenAI Deployment 名稱
AZURE_OPENAI_DEPLOYMENT=gpt-4.1-mini
# Azure OpenAI API 版本
AZURE_OPENAI_API_VERSION=2025-01-01-preview

# JWT
JWT_SECRET=change-me

